services:
  ruview:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "3001:3000"
      - "3002:3001"
      - "5005:5005/udp"

    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./logs:/app/logs
      - ./models:/app/models

    labels:
      ai-tool: "ruview"

    command:
      - /app/sensing-server
      - --http-port
      - "3000"
      - --ws-port
      - "3001"
      - --bind-addr
      - 0.0.0.0
      - --disable-host-validation
      - --ui-path
      - /app/ui
      - --source
      - esp32

    restart: unless-stopped